/* WordSpies — install wall RETIRED per owner ask.
 *
 * The original file put up a full-screen "Add to home screen" wall on
 * every phone visit. Owner asked to remove install prompts everywhere,
 * so this file is now a no-op.  Kept as a shell so any <script src>
 * references from cached HTML don't 404.
 *
 * If we ever want a real native install path back, the recommended
 * route is a discreet setting under Me → Get the app instead of a
 * blocking wall.
 */
(function () { 'use strict'; /* intentionally empty */ })();
